namespace Application.Helpers.PasswordHasher;

public interface IPasswordHasher
{
    //(string Hash, string Salt) Hasher(string password);
    //bool Verify(string password, string hash, string salt);
}
